# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/orari?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@Ppsmith15048!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server configuration
server.port=8090
# Removiamo context-path per servire frontend e API dalla root

# Logging
logging.level.com.orari=DEBUG
logging.level.org.springframework.web=DEBUG

# CORS configuration
cors.allowed-origins=http://localhost:3000

# Application specific properties
app.turni.rules.min-rest-days=2
app.turni.rules.max-consecutive-same-shift=3
